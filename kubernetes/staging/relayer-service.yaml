apiVersion: v1
kind: Service
metadata:
  name: hyperlane-relayer
  namespace: hyperlane
  labels:
    app: hyperlane-relayer
spec:
  type: ClusterIP
  selector:
    app: hyperlane-relayer
  ports:
    - name: http
      port: 9091
      targetPort: 9091
      protocol: TCP
